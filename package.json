{
  "name": "commerce",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "PORT=8000  turbo run build --filter=next-commerce...",
    "dev": "PORT=8000  turbo run dev",
    "start": "PORT=8000  turbo run start",
    "types": "PORT=8000  turbo run types",
    "prettier-fix": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "prettier": "^2.7.1",
    "turbo": "^1.4.6"
  },
  "husky": {
    "hooks": {
      "pre-commit": "turbo run lint"
    }
  },
  "packageManager": "pnpm@7.5.0"
}
